/**
 * Put all your z-indexes here so you can see them side by side and
 * keep track of them.
 */

enum zElements {
  /* Lowest on top */
  modalShader = 1,
  modalWindow,
  /* Highest on the bottom */
}

export default zElements
